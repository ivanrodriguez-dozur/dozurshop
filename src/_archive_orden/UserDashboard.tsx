'use client';
import { createClient } from '@supabase/supabase-js';
import React, { useState } from 'react';
import {
  FaBolt,
  // Este archivo mantiene compatibilidad importando el componente renombrado.
  export { default } from './PerfilDeUsuario';
